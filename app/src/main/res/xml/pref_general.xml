<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:key="constant_mode"
        android:title="@string/constant_mode"
        android:summary="@string/constant_mode_desc"
        android:defaultValue="false" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="time_interval"
        android:title="@string/pref_time_interval"
        android:defaultValue="5"
        android:entries="@array/pref_time_intervals"
        android:entryValues="@array/pref_time_interval_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
